version: "3.3"

services:
  backend-mongo:
    container_name: backend-mongo
    image: "bitnami/mongodb:4.1"
    volumes:
      - "mongo_data:/bitnami"
    env_file:
      - .env
    expose:
      - "29017"


volumes: 
  mongo_data:

networks:
  default:
    external:
      name: fullstack 
